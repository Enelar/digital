<%
for (var k in block_user_profile)
{
  if (!header.hasOwnProperty(k))
    continue;
  v = block_user_profile[k];
%>
<%= DeferRender('main/block_user_profile_element', {url: v, name: k}) %>
<%
}
%>
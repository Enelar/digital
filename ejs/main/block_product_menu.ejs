<%
for (var k in block_product_menu)
{
  if (!block_product_menu.hasOwnProperty(k))
    continue;
  v = block_product_menu[k];
%>
<%= DeferRender('main/block_product_menu_element', {url: v, name: k}) %>
<%
}
%>